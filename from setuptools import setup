from setuptools import setup

setup(
    name="mvt-cli",
    version="0.0.1",
    py_modules=["aliases"],
    include_package_data=True,
    install_requires=["click", google-cloud-bigquery],
    entry_points="""
        [console_scripts]
        mvt=__main__:main
    """,
)